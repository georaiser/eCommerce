
/*
// 1. You clicked the Add button. Travel up the HTML tree to find the table row (<tr>)
const row = event.target.closest("tr");

// 2. Search ONLY inside that exact row for your input box class
const quantity = row.querySelector(".amount-input").value;

// 3. You still need this to know WHICH product you clicked on
const productId = event.target.getAttribute("data-id");


*/